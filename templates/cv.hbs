{{!-- CV template --}}

<!doctype html>
<html>
<head><meta charset="utf-8"><style>body{font-family: Arial}</style></head>
<body>
  <h1>{{profile.name}}</h1>
  <p>{{profile.email}} • {{profile.phone}}</p>
  <h2>Experience</h2>
  <ul>
    {{#each bullets}}
      <li>{{this}}</li>
    {{/each}}
  </ul>
</body>
</html>
